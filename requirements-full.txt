# Full pipeline (segmentation, depth back-projection, OBB, rendering).
# Requires Python >= 3.10. SAM2 + PyTorch are installed separately — see README.
numpy>=1.24,<2.0
scipy>=1.10
opencv-python>=4.8
open3d>=0.17
matplotlib>=3.7
flask>=3.0
pillow>=10.0
python-docx>=1.1    # fill the .docx quote template (견적서 생성)
google-genai>=1.0   # optional — only needed for the Gemini VLM stage
huggingface_hub>=0.24  # setup_models.sh — download SAM2 + Gemma
# Local Gemma 4 VLM fallback (used when no Gemini key). Needs transformers>=5.5 (gemma4).
transformers>=5.5
accelerate>=0.30
# SAM2 segmentation (install after torch; see README for platform notes):
#   pip install git+https://github.com/facebookresearch/segment-anything-2.git
